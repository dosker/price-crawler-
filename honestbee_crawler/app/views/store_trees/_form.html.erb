<%= form_for(@store_tree) do |f| %>
  <% if @store_tree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_tree.errors.count, "error") %> prohibited this store_tree from being saved:</h2>

      <ul>
      <% @store_tree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_title %><br>
    <%= f.text_field :page_title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
